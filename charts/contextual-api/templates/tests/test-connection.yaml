apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "contextual-api.fullname" . }}-test-connection"
  labels:
    {{- include "contextual-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "contextual-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
