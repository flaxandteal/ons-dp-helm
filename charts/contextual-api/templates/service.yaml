apiVersion: v1
kind: Service
metadata:
  name: {{ include "contextual-api.fullname" . }}
  labels:
    {{- include "contextual-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contextual-api.selectorLabels" . | nindent 4 }}
