apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dp-conceptual-alpha.fullname" . }}-test-connection"
  labels:
    {{- include "dp-conceptual-alpha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dp-conceptual-alpha.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
